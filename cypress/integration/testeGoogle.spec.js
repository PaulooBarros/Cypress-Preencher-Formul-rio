describe('Testes em Google', () => {
    beforeEach(() => {
        cy.visit('https://www.google.com'); // Visita o site antes de cada teste
    });

    it('iniciando', function(){
        //cy.get('#APjFqb').type('Teste').should('have.value','Teste')
        //cy.intercept('POST','/log?format=json&hasfast=true&authuser=0').as('iconeCamera')
        //cy.get('.nDcEnd').click('@iconeCamera')

    })
});